<a href="{{ url  }}" class="com-list-item" data-index="{{ index }}>
    <img src="{{ img }}" class="img"/>
    <div class="info">
        <p class="title">{{ title }}</p>
        <p class="author">{{ author }}</p>
        <p class="date">{{ date }}</p>
    </div>
</a>